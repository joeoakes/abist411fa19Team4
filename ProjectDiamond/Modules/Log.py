class Log:

	def __init__(self, originatingAgent, timestamp, currentProcess, processSuccess):
		self.originatingAgent = originatingAgent
		self.timestamp = timestamp
		self.currentProcess = currentProcess
		self.processSuccess = processSuccess

